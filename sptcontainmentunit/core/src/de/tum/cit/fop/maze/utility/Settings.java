package de.tum.cit.fop.maze.utility;

/**
 * The class holds game configuration constants like tile size and scale.
 * These values help control the game world size and appearance.
 */
public class Settings {
    public static int TILE_SIZE = 32;
    public static float SCALE = 1f;
    public static float SCALED_TILE_SIZE = SCALE * TILE_SIZE;
}
